# Fixture: repository with no dev configuration

This directory deliberately has no `.agent-toolkit/dev.md`. The composite action must exit 0
with a single skip message, install no toolchain, and create nothing here.
