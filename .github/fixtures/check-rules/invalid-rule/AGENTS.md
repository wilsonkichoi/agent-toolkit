# Fixture project conventions

Project instructions for the invalid-rule check-rules fixture. Only the rule file is
deliberately broken; everything else here is well formed, so the failure the workflow asserts
can come from nothing else.
