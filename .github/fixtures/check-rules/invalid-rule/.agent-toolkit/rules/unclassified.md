# Unclassified rule

This file declares no `tier`, so rule discovery must hard-stop on it instead of silently
guessing whether it is a rule. Do not add frontmatter here: the fixture exists to prove the
composite action fails a repository that would fail a lifecycle run.
